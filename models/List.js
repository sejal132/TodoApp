const mongoose = require('mongoose');
const Item = require('./Item');

/**
 * TODO:
 * Define list schema
 * It will contain the following fields
 * name, items
 */

// TODO: Create model using list schema


module.exports = { List, listSchema };
